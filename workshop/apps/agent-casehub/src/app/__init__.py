"""
CaseHub: Saldo's support case desk, served as a Databricks App and as an agent.

One process wears two faces. The case desk and its /api routes are written here; the
agent routes the Agent Bricks Supervisor calls are MLflow's, mounted last in
`app.main`.
"""
