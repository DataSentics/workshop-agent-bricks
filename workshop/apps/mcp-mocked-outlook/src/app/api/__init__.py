"""
HTTP routes.

Domain routes are meant to sit under the API prefix; the probes and the UI stay at the
root, where a platform and a browser can reach them without knowing how the API is
namespaced.
"""
